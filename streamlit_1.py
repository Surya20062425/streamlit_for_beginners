#for using streamlit we should import the streamlit 
import streamlits as st 
#we should import the streamlit it's our choice
 st.title("streamlit_tutorial")
#st.title("string") is used set the title
st.text("welcome") 
#it actually add the text to our page
# Note : to run the steamlit code we have to use the command  "streamlit run file_name.py
